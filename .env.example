# Copy to .env.local for local development.
# In production these are set as Vercel environment variables (never commit real values).

# Password for /wc/referrals (shared with Work Canvas staff)
REFERRALS_PASSWORD_WC=

# Password for /miraizm/referrals (shared with Miraizm staff)
REFERRALS_PASSWORD_MIRAIZM=

# Random secret used to sign the 30-day session cookie (e.g. 64 hex chars)
SESSION_SECRET=
